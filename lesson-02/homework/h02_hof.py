"""Домашнее задание. Тема 2: функции высшего порядка и лямбды."""


def hw_05(func, items):
    """map вручную.

    Верните список результатов применения func к каждому элементу items.
    Запрещено использовать встроенный map и генераторы списков — только цикл.

    Примеры:
        hw_05(str.upper, ["a", "b"]) == ["A", "B"]
        hw_05(lambda x: x * 2, [1, 2, 3]) == [2, 4, 6]
        hw_05(str, []) == []
    """
    raise NotImplementedError("Реализуйте hw_05")


def hw_06(predicate, items):
    """filter вручную.

    Верните список элементов items, для которых predicate(element) истинен.
    Запрещено использовать встроенный filter и генераторы списков — только цикл.

    Примеры:
        hw_06(lambda x: x % 2 == 0, [1, 2, 3, 4]) == [2, 4]
        hw_06(str.isupper, ["A", "b", "C"]) == ["A", "C"]
        hw_06(bool, []) == []
    """
    raise NotImplementedError("Реализуйте hw_06")


def hw_07(words):
    """Сортировка слов по последней букве.

    Верните новый список: слова отсортированы по последней букве по алфавиту;
    при совпадении последних букв — по алфавиту целиком. Используйте key
    с лямбдой, возвращающей кортеж (последняя буква, слово).

    Примеры:
        hw_07(["дом", "кот", "жук"]) == ["жук", "дом", "кот"]   # к < м < т
        hw_07(["я", "а"]) == ["а", "я"]
    """
    raise NotImplementedError("Реализуйте hw_07")
