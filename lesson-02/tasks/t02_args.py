"""Тема 2. *args, **kwargs, лямбды и функции высшего порядка."""


def task_07(*args):
    """Произведение произвольного числа аргументов.

    Верните произведение всех переданных числовых аргументов.
    Для пустого вызова верните 1 (нейтральный элемент умножения).

    Примеры:
        task_07(2, 3, 4) == 24
        task_07(5) == 5
        task_07() == 1
        task_07(2, 0.5) == 1.0
    """
    raise NotImplementedError("Реализуйте task_07")


def task_08(**kwargs):
    """Отчёт по именованным аргументам.

    Верните строку "ключ=значение" для всех переданных именованных аргументов,
    отсортированную по ключу, элементы разделены "; ". Для пустого вызова — "".

    Примеры:
        task_08(b=2, a=1) == "a=1; b=2"
        task_08() == ""
        task_08(name="Иван", age=30) == "age=30; name=Иван"
    """
    raise NotImplementedError("Реализуйте task_08")


def task_09(func, args, kwargs):
    """Универсальный проброс аргументов.

    Вызовите func с позиционными аргументами из кортежа args и именованными
    из словаря kwargs (используйте распаковку * и **). Верните результат.

    Примеры:
        def f(a, b, c=0): return a + b + c
        task_09(f, (1, 2), {"c": 3}) == 6
        task_09(max, ([1, 5, 3],), {}) == 5
    """
    raise NotImplementedError("Реализуйте task_09")


def task_10(words):
    """Сортировка слов по длине и алфавиту.

    Верните новый список: слова отсортированы по возрастанию длины,
    при равной длине — по алфавиту. Используйте key с лямбдой, возвращающей
    кортеж (длина, слово).

    Примеры:
        task_10(["кот", "а", "бык", "я"]) == ["а", "я", "бык", "кот"]
        task_10(["банан", "яблоко", "абрикос"]) == ["банан", "яблоко", "абрикос"]
    """
    raise NotImplementedError("Реализуйте task_10")


def task_11(numbers):
    """Квадраты чётных через map/filter.

    Верните список квадратов чётных чисел из numbers. Реализуйте решение
    через композицию map и filter (без генераторов списков и циклов).

    Примеры:
        task_11([1, 2, 3, 4]) == [4, 16]
        task_11([1, 3]) == []
        task_11([]) == []
    """
    raise NotImplementedError("Реализуйте task_11")


def task_12(f, g):
    """Композиция двух функций.

    Верните функцию одного аргумента x, которая вычисляет f(g(x)).

    Примеры:
        task_12(lambda x: x * 2, lambda x: x + 1)(3) == 8   # 2 * (3 + 1)
        task_12(str.upper, str.strip)("  hi  ") == "HI"
    """
    raise NotImplementedError("Реализуйте task_12")
